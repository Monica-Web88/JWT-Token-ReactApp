# JWT Token in React Login Dashboard

## Tech Stack
- Frontend: React
- Backend: Node.js
- Authentication: JWT Token

## Features of the Application
- Demo of a login dashboard with JWT-based authentication (login, refresh, logout)
- Uses a Node.js backend server to handle API requests
- Checks if the token has expired and requests a new refresh token
- Admin user has the option to delete users
